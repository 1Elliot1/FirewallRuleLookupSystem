# repo-root/pytest.ini
[pytest]
minversion = 8.0
addopts = -ra -q
pythonpath = app          # so tests import ruleGenerator…
testpaths =               # <– tell pytest exactly where tests live
    app/tests